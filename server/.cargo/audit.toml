[advisories]
# RUSTSEC-2023-0071: rsa 0.9.10 (Marvin Attack). No fixed upgrade.
# Pulled by sqlx-mysql → sqlx-macros-core (compile-time macro expansion only).
# Discool enables only postgres+sqlite (default-features = false).
# Verified: not in `cargo tree --target x86_64-unknown-linux-gnu`.

# RUSTSEC-2026-0118: hickory-proto 0.25.2 NSEC3 unbounded loop. No fix available.
# RUSTSEC-2026-0119: hickory-proto 0.25.2 CPU exhaustion. Fix in >=0.26.1,
# but blocked by libp2p-mdns 0.48 which pins ^0.25.2.
# Both pulled by libp2p-mdns / libp2p-dns — platform-specific deps NOT compiled
# for x86_64-unknown-linux-gnu. Verified via `cargo tree`.
# Dismissed on GitHub Dependabot as inaccurate / not in active dependency tree.

# CVE-2026-32314 / GHSA-vxx9-2994-q338: yamux <0.13.10 remote panic.
# Patched locally: server/patches/libp2p-yamux removes the vulnerable yamux 0.12.x
# path and uses only yamux 0.13.10+. See server/patches/libp2p-yamux/README.md.

# RUSTSEC-2024-0436: paste 1.0.15 unmaintained (compile-time proc-macro, no vuln).
# Pulled by netlink-packet-core 0.8.1 → rtnetlink → if-watch → libp2p.
ignore = ["RUSTSEC-2023-0071", "RUSTSEC-2026-0118", "RUSTSEC-2026-0119", "RUSTSEC-2024-0436"]
